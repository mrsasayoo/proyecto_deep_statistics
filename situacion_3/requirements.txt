# ============================================================================
# Requirements — Proyecto Situación 3: Portafolio Hipotecario
# ============================================================================
# Instalación: pip install -r requirements.txt
# Nota: Instalar en AMBAS máquinas (Master + Worker) para Ray distribuido

# Core científico
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
scipy>=1.10.0
pyarrow>=14.0.0            # Lectura/escritura Parquet (columnar, comprimido)

# Machine Learning
scikit-learn>=1.3.0

# Deep Learning
torch>=2.0.0               # PyTorch para VAE (Variational Autoencoder)

# Análisis Factorial
factor-analyzer>=0.5.0     # AFE (Análisis Factorial Exploratorio)
# semopy>=2.0.0            # SEM / AFC (opcional, requiere compilación C)

# Computación Distribuida
ray[default]>=2.9.0        # Framework de paralelización Master-Worker

# Visualización
matplotlib>=3.7.0
seaborn>=0.12.0

# Utilidades
tqdm>=4.65.0               # Barras de progreso en terminal
rich>=13.0.0               # Consola mejorada (colores, tablas, spinners)
psutil>=5.9.0              # Monitoreo de RAM, CPU, disco
joblib>=1.3.0              # Paralelización local y persistencia de modelos

# Notebooks (opcional)
jupyter>=1.0.0
ipywidgets>=8.0.0
